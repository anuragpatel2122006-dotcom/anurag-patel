#include <stdio.h>
float main()
{   float a,b;
    printf("enetr kg which you want to convert into gram :");
    scanf("%f",&a);
    b = a * 1000;
    printf("%f kg is %f gram ",a,b);
    return 0;
}