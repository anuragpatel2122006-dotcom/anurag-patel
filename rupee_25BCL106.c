#include <stdio.h>
int main()
{   int a,b,c;
    printf("enetr ruppe amount which you want to convert into dollar:");
    scanf("%d",&a);
    b = a / 48;
    printf("%d ruppe is %d dollar",a,b);
    return 0;
}