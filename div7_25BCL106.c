#include <stdio.h>
float main()
{ float NUM,R;
    printf("enter a number; ");
    scanf("%f",&NUM);
    R = NUM/7 ;

    if (R==0)
       printf("%f is divisible by 7",NUM);
    else
       printf("%f is not divisible by 7",NUM);
    return 0 ;
}   













}