#include <stdio.h>
float main()
{   float area , radius ;
    printf("enetr radius of circle");
    scanf("%f",&radius);
    area = 22 * (radius * radius) /7 ;
    printf("area of circle is %f",area);
    return 0;
}